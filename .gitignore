# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
pnpm-debug.log*

# environment
.env
.env.production
.env.local

# macOS
.DS_Store

# editors
.vscode/*
!.vscode/extensions.json
.idea/

# private tooling — symlink to a separate checkout, never committed here
/tools

# Claude Code local config (agents and hooks live outside this repo)
.claude/settings.local.json

# Agents and project docs live in the private tooling repo, reached via tools/.
# Ignored here so a file written to the old path never lands in a public commit.
.claude/agents/
/docs
