---
import { SITE, NAV } from '../config';
import ThemeToggle from './ThemeToggle.astro';
import Avatar from './Avatar.astro';

const path = Astro.url.pathname;
const isActive = (href: string) => (href === '/' ? path === '/' : path.startsWith(href));
---

<header class="site-header sticky top-0 z-50 border-b border-default">
  <div class="mx-auto flex h-16 max-w-6xl items-center gap-2 px-4 sm:gap-4 sm:px-6">
    <a
      href="/"
      class="flex min-w-0 items-center gap-2.5"
      aria-label={`${SITE.title} blog ${SITE.byline} — home`}
    >
      <Avatar size={32} eager class="shrink-0 ring-1 ring-[var(--border)]" />
      <span class="min-w-0 leading-tight">
        <span class="block truncate font-semibold tracking-tight">
          <span class="text-accent">MS</span>DEVBUILD
          <span class="font-normal text-muted">/ blog</span>
        </span>
        <span class="block truncate text-[0.7rem] text-muted">{SITE.alternateName} · {SITE.byline}</span>
      </span>
    </a>

    <nav class="ml-auto hidden items-center gap-1 sm:flex" aria-label="Main">
      {NAV.map(({ label, href }) => (
        <a
          href={href}
          aria-current={isActive(href) ? 'page' : undefined}
          class:list={[
            'rounded-md px-3 py-2 text-sm transition-colors hover:bg-subtle',
            isActive(href) ? 'font-medium text-accent' : 'text-muted',
          ]}
        >
          {label}
        </a>
      ))}
    </nav>

    <div class="ml-auto flex shrink-0 items-center gap-1 sm:ml-0">
      <!-- Opens the ⌘K palette; falls back to /search if JS is unavailable. -->
      <a
        href="/search/"
        data-open-search
        class="rounded-md p-2 transition-colors hover:bg-subtle"
        aria-label="Search (⌘K)"
      >
        <svg class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
          <circle cx="11" cy="11" r="7" /><path d="m20 20-3.5-3.5" />
        </svg>
        <!-- Anchor text for crawlers, which read markup and do not credit
             aria-label. The aria-label still wins for assistive tech, so this
             is not announced twice. -->
        <span class="sr-only">Search</span>
      </a>
      <!-- Install as an app (PWA). Hidden until the browser reports the site is
           installable, or on iOS where it opens Add-to-Home-Screen steps. Solid
           accent pill so it stands out and invites the install.

           Icon only on phones: with the label, this button is 126px wide, which
           on a 390px iPhone pushed the burger past the viewport edge — and since
           the page clips horizontal overflow, the menu simply vanished. The
           aria-label carries the name for anyone who needs it. -->
      <button
        id="pwa-install"
        type="button"
        hidden
        class="btn btn-accent btn-sm max-sm:size-9 max-sm:gap-0 max-sm:px-0"
        aria-label="Install app"
      >
        <svg class="size-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M12 3v11" /><path d="m8 11 4 4 4-4" /><path d="M5 20h14" />
        </svg>
        <span class="max-sm:hidden">Install app</span>
      </button>
      <ThemeToggle />
      <button
        id="menu-toggle"
        type="button"
        class="rounded-md p-2 transition-colors hover:bg-subtle sm:hidden"
        aria-label="Open menu"
        aria-expanded="false"
        aria-controls="mobile-nav"
      >
        <svg class="size-5" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" aria-hidden="true">
          <path d="M4 7h16M4 12h16M4 17h16" />
        </svg>
      </button>
    </div>
  </div>

  <nav id="mobile-nav" class="hidden border-t border-default px-4 py-2 sm:hidden" aria-label="Mobile">
    {NAV.map(({ label, href }) => (
      <a href={href} class="block rounded-md px-3 py-2.5 text-sm transition-colors hover:bg-subtle">
        {label}
      </a>
    ))}
  </nav>
</header>

<style>
  /* Fully opaque, no backdrop-filter. A translucent + blurred sticky header
     forces the browser to recompute a gaussian blur over the scrolling content
     on every frame; on Chrome — especially Retina Macs — that shows up as scroll
     jank and flicker. A solid header composites once and scrolls perfectly. */
  .site-header {
    background: var(--bg);
  }
</style>

<script>
  function initMenu() {
    const button = document.getElementById('menu-toggle');
    const nav = document.getElementById('mobile-nav');
    button?.addEventListener('click', () => {
      const open = nav?.classList.toggle('hidden') === false;
      button.setAttribute('aria-expanded', String(open));
      button.setAttribute('aria-label', open ? 'Close menu' : 'Open menu');
    });
  }

  initMenu();
  document.addEventListener('astro:after-swap', initMenu);

  // Any [data-open-search] element opens the command palette instead of
  // navigating; the href stays as a no-JS fallback.
  function initSearchTriggers() {
    document.querySelectorAll('[data-open-search]').forEach((el) => {
      if ((el as HTMLElement).dataset.searchBound === '') return;
      (el as HTMLElement).dataset.searchBound = '';
      el.addEventListener('click', (e) => {
        e.preventDefault();
        window.dispatchEvent(new Event('open-search'));
      });
    });
  }
  initSearchTriggers();
  document.addEventListener('astro:page-load', initSearchTriggers);
</script>
